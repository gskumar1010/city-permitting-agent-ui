apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mechanic.fullname" . }}-test-connection"
  labels:
    {{- include "mechanic.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mechanic.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
